import Typography from "antd/lib/typography";

const InvalidWing: React.FC = () => {
  return <Typography.Title level={1} style={{textAlign:"center", verticalAlign:"middle"}}>Welcome to Resources Portal</Typography.Title>;
};

export default InvalidWing;
